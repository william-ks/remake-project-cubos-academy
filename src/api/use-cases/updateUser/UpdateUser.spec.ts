import { describe, expect, test } from 'vitest';

describe('Testing update user => name or/and password', () => {
    test('update with all data', async () => {
        expect(200).toBe(200)
    })
})